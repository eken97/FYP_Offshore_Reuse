# Runtime dependencies for the post-processing pipeline and figure builders.
# Python 3.10+ (the code uses PEP 604 `X | None` annotations).
numpy
pandas
matplotlib
rainflow          # cycle counting (ASTM E1049) used by rainflow_hist.py
openpyxl          # reading/writing the .xlsx appendix tables
python-docx       # build_static_appendix.py only

# Optional. Used solely by outb_reader.py's self-check to cross-validate the
# targeted .outb reader against the reference implementation. The pipeline
# does not need it.
# openfast-toolbox
